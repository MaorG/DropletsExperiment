function res = masks2Label(data,parameters)

props = parseParams(parameters);

maskNamesAndLabels = data.(props.masks);

masks = {};

for mi = 1:numel(naskNames)
    masks{mi} = data.(maskNamesAndLabels{mi}{1})
end


res = zeros(size(masks


res = (map == val);

end

function props = parseParams(v)
% default:
props = struct(...
    'masks',{{'LiveMask' 1} {'deadMask' 2}}...
    );

for i = 1:numel(v)
    
    if (strcmp(v{i}, 'masks'))
        props.map = v{i+1};
    end
end

end